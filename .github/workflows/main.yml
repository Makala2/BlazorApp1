name: parse json

on:
  push:
    branches: [ "master" ]
  #pull_request:
  #  branches: [ "master" ]

jobs:
  parse-json-file:
    runs-on: [self-hosted, Windows, X64] 
    #env: 
    #  ENV: $GITHUB_ENV

    steps:
    - uses: actions/checkout@v4   
    
    - name: read json info
      #id: get_json
      run: |
        $input = Get-Content -Path ".\.github\workflows\config\target.json" | ConvertFrom-Json
        Write-Host $input[host]
        Write-Host $input.data.host 
        Write-Host DEVSRV=$input[host]
        Write-Host DEVSRV=$input | ConvertFrom-Json .host >> $GITHUB_ENV

    - name: print DevSrv
      run: |
        Write-Host "Development Server ${{ env.DEVSRV }}"
        Write-Host "Development Server $DEVSRV"
        
